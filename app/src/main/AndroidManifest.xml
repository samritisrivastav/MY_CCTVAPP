<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.toshiba.cctvapp">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".connecting_page" />
        <activity android:name=".Home_page" />
        <activity android:name=".Live_preview" />
        <activity android:name=".Device_list" />
        <activity android:name=".Saved_files" />
        <activity android:name=".Remote_playack" />
        <activity android:name=".signup"></activity>
    </application>

</manifest>